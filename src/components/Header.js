import React from 'react';
//Components
import TopNavbar from './TopNavbar';

const Header = () => {
    return (
        <header>
           <TopNavbar/>
        </header>
    );
};

export default Header;
